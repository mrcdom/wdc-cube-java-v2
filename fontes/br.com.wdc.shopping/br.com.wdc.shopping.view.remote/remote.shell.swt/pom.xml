<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>br.com.wdc.shopping</groupId>
        <artifactId>br.com.wdc.shopping.view.remote</artifactId>
        <version>1.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>remote.shell.swt</artifactId>
    <packaging>jar</packaging>

    <name>WeDoCode Shopping View Remote - SWT Shell</name>
    <description>SWT desktop shell communicating with remote.host via WebSocket — reuses swt.commons views with remote state</description>

    <properties>
        <swt.version>3.128.0</swt.version>
    </properties>

    <dependencies>
        <!-- Shared SWT view implementations -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>swt.commons</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Remote bridge (HostClient, ViewStateSnapshot, HostResponse) -->
        <dependency>
            <groupId>br.com.wdc.framework</groupId>
            <artifactId>remote.bridge.java</artifactId>
        </dependency>

        <!-- Presentation (ViewState classes) -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>br.com.wdc.shopping.presentation</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- SWT platform-specific (macOS arm64 — platform profiles add others) -->
        <dependency>
            <groupId>org.eclipse.platform</groupId>
            <artifactId>org.eclipse.swt.cocoa.macosx.aarch64</artifactId>
            <version>${swt.version}</version>
        </dependency>

        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>linux-x86_64</id>
            <activation>
                <os>
                    <family>unix</family>
                    <name>linux</name>
                    <arch>amd64</arch>
                </os>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>org.eclipse.platform</groupId>
                    <artifactId>org.eclipse.swt.gtk.linux.x86_64</artifactId>
                    <version>${swt.version}</version>
                </dependency>
            </dependencies>
        </profile>
        <profile>
            <id>win32-x86_64</id>
            <activation>
                <os>
                    <family>windows</family>
                    <arch>amd64</arch>
                </os>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>org.eclipse.platform</groupId>
                    <artifactId>org.eclipse.swt.win32.win32.x86_64</artifactId>
                    <version>${swt.version}</version>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

</project>
