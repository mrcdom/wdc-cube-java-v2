<?xml version="1.0" encoding="UTF-8"?>
<configuration>

	<property name="LOG_DIR" value="/Users/mrcdom/Works/WeDoCode/wdc-cube-java-v2/fontes/br.com.wdc.cube.backend/work/log" />
	<property name="LOG_PATTERN" value="%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" />

	<!-- Console Appender -->
	<appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>${LOG_PATTERN}</pattern>
		</encoder>
	</appender>

	<!-- File Appender -->
	<appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${LOG_DIR}/server.log</file>
		<encoder>
			<pattern>${LOG_PATTERN}</pattern>
		</encoder>
		<rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
			<fileNamePattern>${LOG_DIR}/server.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
			<maxFileSize>10MB</maxFileSize>
			<maxHistory>7</maxHistory>
		</rollingPolicy>
	</appender>

	<!-- Javalin Logger -->
	<logger name="io.javalin" level="INFO" />

	<!-- Jetty Logger -->
	<logger name="org.eclipse.jetty" level="WARN" />

	<!-- jOOQ SQL Logger (ativado por database.logSql=true) -->
	<logger name="org.jooq.tools.LoggerListener" level="DEBUG" />

	<!-- WeDoCode Loggers -->
	<logger name="br.com.wdc" level="DEBUG" />
	<logger name="br.com.wdc.cube.backend.controller.WebCacheController" level="WARN" />
	<logger name="br.com.wdc.cube.backend.BackendServer" level="WARN" />

	<!-- Root Logger -->
	<root level="INFO">
		<appender-ref ref="CONSOLE" />
		<appender-ref ref="FILE" />
	</root>

</configuration>
