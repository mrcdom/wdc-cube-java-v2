# Ignore runtime data and build outputs
data/
log/
temp/
frontend/*

# Track version-controlled frontend contexts
!frontend/api.docs/

# Track config files
!config/

# Local overrides with secrets (passwords, tokens) — never commit
config/application.local.toml
